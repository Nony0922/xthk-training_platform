server:
  port: 8080

spring:
  config:
    import: optional:classpath:application-local.yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/training_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 242539

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.entity
  configuration:
    map-underscore-to-camel-case: true

# 讯飞星火大模型
spark:
  api-password: ${SPARK_API_PASSWORD:}
  api-url: https://spark-api-open.xf-yun.com/v1/chat/completions
  model: 4.0Ultra
  enabled: true
  timeout-seconds: 45
